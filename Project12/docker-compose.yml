version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: live-monitor-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
