server:
  port: 9002

spring:
  application:
    name: customs-service
  datasource:
    url: jdbc:sqlite:data/customs.db
    driver-class-name: org.sqlite.JDBC
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
